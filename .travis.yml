language: rust
rust:
  - nightly
  
cache:
  directories:
    - $HOME/qemu

before_script:
  - bash -e travis-qemu.sh
  - export PATH=$PATH:$HOME/qemu/bin

script:
  - cargo xbuild --verbose --all

install:
  - cargo install bootimage
  - cargo xbuild --verbose --all